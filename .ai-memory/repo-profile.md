# Repo Profile — lattix-xfrontier (committed, repo-safe)

> Generated by memory-export from `50-Memory/Repos/lattix-xfrontier/repo-profile.md` on 2026-06-04. Do not edit by hand; edit the vault source.

# Repo: lattix-xfrontier

Canonical profile. `memory-export` mirrors this into the repo's committed `.ai-memory/repo-profile.md` (repo-safe).

## Purpose
Secure, local-first multi-agent orchestration platform (AGPL-3.0-or-later).

## Stack
Python 3.12+ (hatchling, `lattix-frontier`): FastAPI, LangGraph (+postgres checkpoint), NATS, Presidio, hvac/Vault, OPA, psycopg/pgvector, redis, structlog, OpenTelemetry. Frontend `apps/frontend/`: Next.js (webpack), eslint, vitest. Infra: Docker/K8s, Helm, Envoy, Biscuit.

## Build & test
`make test` · `make lint` · `make typecheck` · `make policy-test` · `make local-up`/`local-down` · `make stack-up`/`stack-down` · `make bootstrap` · `make health`. Frontend: `npm run test|lint|build`.

## Architecture
4 layers: LangGraph orchestration → MAF guardrail filters → MAF ChatAgents + A2A → infra/security. Surfaces: `apps/backend/` (control plane), `apps/workers/` (runtime), `frontier_runtime/` (runtime + memory pipeline), `packages/contracts/`.

## Conventions
More-specific `AGENTS.md` wins; validate workflows; preserve `pull_request` + `push`-to-`main` validation; issues in Linear (`FRONT-*`).

## Current state
Additive cognitive MVP (`frontier/goal`, `frontier/evidence`, …) → columnar cognitive target. See decision [[mem-20260604-1705-xfrontier-memory-architecture]].
