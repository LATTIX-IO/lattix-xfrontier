# Public sample agents

These sample agents are safe to ship in the public repository and act as the default seed assets for local-first development.

Private or proprietary agent definitions should live outside the FOSS tree and can be loaded by setting `FRONTIER_AGENT_ASSETS_ROOT`.
