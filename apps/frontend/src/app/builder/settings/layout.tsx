import { BuilderSettingsLayoutShell } from "@/components/builder-settings-layout-shell";

export default function BuilderSettingsLayout({ children }: { children: React.ReactNode }) {
  return <BuilderSettingsLayoutShell>{children}</BuilderSettingsLayoutShell>;
}