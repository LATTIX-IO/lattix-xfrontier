[allowlist]
  # Allow redaction placeholders (e.g. "[REDACTED]", "[REDACTED_TOKEN]") without excluding whole source files.
  regexes = [
    "\\[REDACTED[^\\]]*\\]"
  ]
  paths = [
    "(?i)\\..*\\.env",
    "apps/frontend/src/lib/frontier-node-catalog.ts"
  ]
